meta {
  name: Get a statement by its unique id
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/link/:statementId
  body: none
  auth: bearer
}

params:path {
  statementId: 56aa8f11-c285-4426-9f1e-86627460fb70
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{ADMIN_ACCESS_TOKEN}}
}

settings {
  encodeUrl: true
  timeout: 0
}

docs {
  Retrieve statement metadata (does not return binary PDF). Use the `statementId` returned at upload time.
}
